# Stability Check — 2026-04-21-0855

Generated by `bin/dev/stability-check.sh` (119s) — composes
existing per-aspect scripts. See the script header for delegation.

## 🟡 ATTENTION (2)
- Docker disk: 156 GB used — run `docker system prune` (per CLAUDE.md)
- Local Sonar at :9000 is DOWN — start with `docker compose --profile admin up -d sonarqube`

## ℹ️  Info (18)
- healthcheck-all returned non-zero (services likely not running — OK if not needed)
- iris-service: latest main pipeline = running (in flight)
- iris-ui: latest main pipeline = running (in flight)
- iris-ui: 2/5 recent main pipelines failed (trend)
- UI: 0 critical, 0 high CVEs
- Trivy: binary not installed — skip (brew install trivy)
- UI bundle delta: stats.json not found (run `npm run build -- --stats-json` first)
- Lighthouse: perf=64 a11y=94 bp=96 seo=90 (Δ 3/0/0/0)
- Local Sonar :9000 DOWN — freshness check skipped
- UI: 1 console.* call(s) (≤3 acceptable; telemetry sink + main bootstrap excluded)
- Terraform: all 4 modules validate + formatted
- All upstream images pinned (no :latest)
- iris-service: 1 MR(s) currently open
- iris-service: 27 `allow_failure: true` shields — review per CLAUDE.md "Pipelines stay green"
- iris-ui: 14 `allow_failure: true` shields — review per CLAUDE.md "Pipelines stay green"
- Manual CI jobs not triggered (use --trigger-manual to opt in)
- Manual jobs never run on this pipeline: smoke-test,mutation-test,semgrep,build-native,compat-sb3-java17,compat-sb3-java21,compat-sb4-java17,compat-sb4-java21
- Trend: 🔴 0→0 (Δ0), 🟡 2→2 (Δ0) (steady or improving)
